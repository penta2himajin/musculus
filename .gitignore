# Rust
/target
.cargo-home/

# Vendored / fetched artifacts (model weights and evaluation data arrive
# via scripts/, never committed — AGENTS.md, Prohibitions #1)
/vendor
/data
/models
*.onnx
*.sbv2

# Evaluation audio artifacts (hashes and metadata go in docs/benchmarks/)
*.wav

# Local environment
.env
.env.*
!.env.sample

# sbv2-api reference clone (MIT; read-only reference for ADR-0003)
.sbv2-reference/
.irodori-reference/
.irodori-tts-ref/

# Blind A/B engine mapping (revealed after the listener scores)
/ab-test/key.ndjson

# macOS
.DS_Store
